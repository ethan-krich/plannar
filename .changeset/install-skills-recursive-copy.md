---
"plannar": patch
---

Added test coverage verifying `install-skills` copies entire skill folders (including subdirectories like `references/`) and updated docs to document recursive copy behavior.
