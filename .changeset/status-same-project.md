---
"@plannar/plannar": minor
---

`plannar status` now reports whether the running editor belongs to the current project or a different one by comparing the embedded `plannar-root` meta tag against the resolved plannar folder.
